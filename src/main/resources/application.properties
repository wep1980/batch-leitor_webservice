spring.datasource.jdbcUrl=jdbc:mysql://localhost:3306/spring_batch?serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=root
spring.batch.jdbc.initialize-schema=always

# Configuracao que corresponde ao tamanho do pageSize da Api que tem uma restrição de 20 itens por pagina
chunkSize=20
pageSize=20
#Limite para ler ate 40 registros e depois encerrar
limit=40
